import { SearchProduitsPipe } from './search-produits.pipe';

describe('SearchProduitsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchProduitsPipe();
    expect(pipe).toBeTruthy();
  });
});
